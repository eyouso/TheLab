{
  "name": "the-lab",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend/mobile\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend/mobile\""
  },
  "devDependencies": {
    "concurrently": "^6.5.1"
  }
}
